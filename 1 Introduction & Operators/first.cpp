#include <iostream>
using namespace std;
int main(void);
{
    int a,b,c;
    cout<<"Enter the Value of a: ";
    cin>>a;
    cout<<"Enter the Value of b: ";
    cin>>b;

    //operation perform
    c = a + b;
    cout<<"The Sum of a & b is "<<c;
    return 0;
}
