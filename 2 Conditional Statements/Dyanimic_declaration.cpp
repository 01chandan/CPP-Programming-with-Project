#include <iostream>
using namespace std;
int main(void)
{
    system("cls");
    //Ex 1: sum of two numbers
    int a=5, b=5;
    if(true)
    {
        int c = a + b; // dyanimic declaration is used for memory utilization
        cout<<"Ans is :"<<c;
    }
    return 0;
}